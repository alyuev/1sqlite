# ============================================================
#  1sqlite — что НЕ включаем в публичный репозиторий
# ============================================================

# --- Проприетарный Microsoft Visual C++ 6.0 (нельзя перевыкладывать) ---
#     Скачать портативную сборку отдельно и положить в tools/MSVC600/ (см. README)
/tools/

# --- Референсные исходники и архивы (для сборки не нужны) ---
/sqlite1c-googlecode/
/sqlite1c_346/
/sqlite-new/
/sqlite-3502/
/_cltest/
/_mfctest/
*.zip
*.7z

# --- Устаревшая ветка 3.50.2 и вспомогательные скрипты ---
/patch_3502*.ps1
/clone_1sqlite.cmd

# --- Старый git-репозиторий внутри SQL_DBF и его архив ---
/SQL_DBF/.git/
/.git_SQL_DBF_archive/

# ============================================================
#  Артефакты сборки
# ============================================================
/SQL_DBF/build/
/SQL_DBF/obj/
/StrMatch/build/
/StrMatch/obj/
/Spellfix/build/
/Spellfix/obj/
*.obj
*.dll
*.res
*.exp
*.ilk
*.pdb
*.bak
*.orig
# (импорт-либы *.lib в _1Common и zlib НУЖНЫ — НЕ игнорируем)

# ============================================================
#  Python (вспомогательные инструменты анализа .ert/.MD)
# ============================================================
__pycache__/
*.pyc
.venv/

# ============================================================
#  1С тест-база: рантайм-артефакты (сама база сохраняется)
# ============================================================
/1CTestBase/*.LCK
/1CTestBase/*.lck
/1CTestBase/test_log.txt
/1CTestBase/*.clean
/1CTestBase/SYSLOG/
/1CTestBase/NEW_STRU/
